title: Executive and Local Governments 
new-page: true
is-card: true
card-image: img/vorwarts_2.jpg

@rm_main

Changing the leadership of the party is painful, but perhaps it is necessary to adapt to changing circumstances.

Removing advisors will increase dissent from their faction of the party, while selecting advisors will increase a faction's strength.

- @government: Federal Government
- @local: Federal States
- @easy_discard

@local

The list of the Govermnents of Merkopia. 

- @merkia: Republic of Merkia
- @getewehn: The City of Getewehn
- @yougen: State of Yougen
- @percil_reinenburg: Free State of Percil-Reinenburg
- @merfeld: State of Merfeld
- @mirfallen: Republic of Mirfallen
- @mirvieren: State of Mirvieren
- @tippleford: Free Region of Tippleford
- @lurek: Republic of Lurek

@merkia
subtitle: [? if merkia_spd == 1 and merkia_timer == 0: Available action of the local Government ?]
on-arrival: {!
function allocateSeats(percentages, totalSeats = 250) {
    const exactSeats = {};
    const flooredSeats = {};
    const remainders = [];

    let totalFlooredSeats = 0;
    for (const party in percentages) {
        const exact = (percentages[party] * totalSeats) / 100;
        const floored = Math.floor(exact);
        const remainder = exact - floored;

        exactSeats[party] = exact;
        flooredSeats[party] = floored;
        totalFlooredSeats += floored;

        remainders.push({ party, remainder });
    }

    const seatsToDistribute = totalSeats - totalFlooredSeats;
    remainders.sort((a, b) => b.remainder - a.remainder);

    for (let i = 0; i < seatsToDistribute; i++) {
        flooredSeats[remainders[i].party]++;
    }

    return flooredSeats;
}

const inputPercentages = {
    spd_mer: Q.spd_mer,
    asum_mer: Q.asum_mer,
    spm_mer: Q.spm_mer,
    sapd_mer: Q.sapd_mer,
    ddp_mer: Q.ddp_mer,
    ppu_mer: Q.ppu_mer,
    z_mer: Q.z_mer,
    dvp_mer: Q.dvp_mer,
    dnvp_mer: Q.dnvp_mer,
    nsdap_mer: Q.nsdap_mer,
    gnvp_mer: Q.gnvp_mer,
    blpm_mer: Q.blpm_mer,
    other_mer: Q.other_mer
};

const allocatedSeats = allocateSeats(inputPercentages);

for (const party in allocatedSeats) {
    Q[party] = parseFloat(((allocatedSeats[party] * 100) / 250).toFixed(1));
}
!}
on-display: {!
data = [];
if (Q.sapd_formed && Q.sapd_r) {
    data.push({
            "id": "sapd",
            "legend": "KPM",
            "name": "KPM",
            "seats": Math.round(Q.sapd_mer*2),
            });
}
if (Q.asum_formed && Q.asum_r) {
    data.push({
            "id": "asum",
            "legend": "ASUM",
            "name": "ASUM",
            "seats": Math.round(Q.asum_mer*2),
            });
}
data = data.concat([
    {
        "id": "spd",
        "legend": Q.spd_name,
        "name": Q.spd_name,
        "seats": Math.round(Q.spd_mer*2),
    },
    ]);
if (Q.spm_formed && Q.spm_r) {
    data.push({
            "id": "spm",
            "legend": "SPM",
            "name": "SPM",
            "seats": Math.round(Q.spm_mer*2),
            });
}
data = data.concat([
    {
        "id": "ddp",
        "legend": Q.ddp_name,
        "name": Q.ddp_name,
        "seats": Math.round(Q.ddp_mer*2),
    },
    {
        "id": "z",
        "legend": "GDSU",
        "name": "GDSU",
        "seats": Math.round(Q.z_mer*2),
    },
    ]);
if (Q.blpm_formed && Q.blpm_r) {
    data.push({
            "id": "blpm",
            "legend": "BLPM",
            "name": "BLPM",
            "seats": Math.round(Q.blpm_mer*2),
            });
}
data = data.concat([
    {
        "id": "bvp",
        "legend": "PPU",
        "name": "PPU",
        "seats": Math.round(Q.ppu_mer*2),
    },
    {
        "id": "dvp",
        "legend": Q.dvp_name,
        "name": Q.dvp_name,
        "seats": Math.round(Q.dvp_mer*2),
    },
    {
        "id": "other",
        "legend": "Other",
        "name": "Other",
        "seats": Math.round(Q.other_mer*2),
    },
]);
if (Q.gnvp_formed && Q.gnvp_r) {
    data.push({
            "id": "gnvp",
            "legend": "GNVP",
            "name": "GNVP",
            "seats": Math.round(Q.gnvp_mer*2),
            });
}
data = data.concat([
    {
        "id": "dnvp",
        "legend": "MNVP",
        "name": "MNVP",
        "seats": Math.round(Q.dnvp_mer*2),
    },
    {
        "id": "nsdap",
        "legend": "NSDAP",
        "name": "NSDAP",
        "seats": Math.round(Q.nsdap_mer*0.96),
    },
]);


if (window && d3) {
    var width = 500;
    var height = 500;
    var screenWidth = document.getElementById('content').offsetWidth;
    if (screenWidth < width - 50) {
        width = screenWidth - 50;
        height = width;
        document.getElementById("saxony_landtag").style.height = screenWidth/2 + "px";
    }
    var parliament = d3.parliament();
    parliament.width(width).height(height).innerRadiusCoef(0.4);
    parliament.enter.fromCenter(true).smallToBig(true);
    parliament.exit.toCenter(false).bigToSmall(true);
    d3.select("#saxony_landtag").datum(data).call(parliament);
}
!}

**Caversket of the Republic of Merkia:**

Governing Coalition :

-@merkia_action: Policies in Merkia
-@local: Return

@merkia_action
unavailable-subtitle: We are not in the Merkian Government 
choose-if: merkia_spd == 1

----- Actions Merkia ----------


////getewehn


@getewehn
subtitle: [? if getewehn_spd == 1 and getewehn_timer == 0: Available action of the local Government ?]
view-if: getewehn_independent == 1

The City of Getewehn:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@getewehn_action: Policies in Getewehn
-@local: Return

@getewehn_action
unavailable-subtitle: We do not hold the mayorat of Getewehn
choose-if: getewehn_spd == 1

----- Actions getewehn ----------


////lurek


@lurek
subtitle: [? if lurek_spd == 1 and lurek_timer == 0: Available action of the local Government ?]
view-if: lukek_federation == 1

The Republic of Lurek:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@lurek_action: Policies in Lurek
-@local: Return

@lurek_action
unavailable-subtitle: We do not hold the mayorat of Lurek
choose-if: lurek_spd == 1

----- Actions Lurek ----------


////yougen


@yougen
subtitle: [? if yougen_spd == 1 and yougen_timer == 0: Available action of the local Government ?]

State of Yougen:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@yougen_action: Policies in Yougen
-@local: Return

@yougen_action
unavailable-subtitle: We are not in the Yougen Government 
choose-if: yougen_spd == 1

----- Actions Merkia ----------


////percil-reinenburg


@percil_reinenburg
subtitle: [? if percil_spd == 1 and percil_timer == 0: Available action of the local Government ?]

Free State of Percil-Reinenburg:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@percil_action: Policies in Percil-Reinenburg
-@local: Return

@percil_action
unavailable-subtitle: We are not in the Percilian-Reinenburgian Government 
choose-if: percil_spd == 1

----- Actions Merkia ----------


////merfeld


@merfeld
subtitle: [? if merfeld_spd == 1 and merfeld_timer == 0: Available action of the local Government ?]

State of Merfeld:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@merfeld_action: Policies in Merfeld
-@local: Return

@merfeld_action
unavailable-subtitle: We are not in the Merfeld Government 
choose-if: merfeld_spd == 1

----- Actions Merkia ----------


/////mirfallen


@mirfallen
subtitle: [? if mirfallen_spd == 1 and mirfallen_timer == 0: Available action of the local Government ?]

Republic of Mirfallen:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@mirfallen_action: Policies in Mirfallen
-@local: Return

@mirfallen_action
unavailable-subtitle: We are not in the Mirfallen Government 
choose-if: mirfallen_spd == 1

----- Actions Merkia ----------


////mirvieren


@mirvieren
subtitle: [? if mirvieren_spd == 1 and mirvieren_timer == 0: Available action of the local Government ?]

State of Mirvieren:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@mirvieren_action: Policies in Mirvieren
-@local: Return

@mirvieren_action
unavailable-subtitle: We are not in the Mirvish Government 
choose-if: mirvieren_spd == 1

----- Actions Merkia ----------


////tippleford


@tippleford
subtitle: [? if tippleford_spd == 1 and tippleford_timer == 0: Available action of the local Government ?]

Free Region of Tippleford:

----- PARLIMENT IMAGINE -------------

Governing Coalition :

-@tippleford_action: Policies in Tippleford
-@local: Return

@tippleford_action
unavailable-subtitle: We are not in the Tipplefordian Government 
choose-if: tippleford_spd == 1

----- Actions Merkia ----------

@government

  government
